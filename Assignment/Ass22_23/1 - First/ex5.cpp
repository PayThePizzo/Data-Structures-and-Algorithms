//
// Created by PayThePizzo on 11/22/2022.
//
#include "header.h"

using namespace std;

// Ai campionati mondiali di maratona il numero di partecipanti è così alto che
// occorre organizzare la gara in due turni in giorni diversi.
//
// Si costruiscono così le due classifiche, una per turno,
// ciascuna delle quali riporta i partecipanti a quel turno in ordine di arrivo e
// il tempo corrispondente.
//
// Dopo le gare, SENZA unificare le due classifiche,
// si vuole avere una tecnica efficiente per determinare, dato una posizione k,
// l'atleta che si è piazzato in quella posizione nella classifica generale.
// ovvero che ha ottenuto il k-mo tempo più basso

int estrai(const std::vector<int>& classifica1, const std::vector<int>& classifica2, int k){
    return 0;
}